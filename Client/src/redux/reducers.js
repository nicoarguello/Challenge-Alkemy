import { } from "./actions"

const inicialState = {
    users : {}
}

function rootReducer(state= inicialState, action) {
    switch(action.type) {

        default:
            return state
    }
}

export default rootReducer;